generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model movies {
  id           Int      @id @default(autoincrement())
  title        String
  genre        String?
  thumbnailUrl String?
  videoUrl     String
  createdAt    DateTime @default(now())
}